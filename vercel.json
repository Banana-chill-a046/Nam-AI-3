{
  "version": 2,
  "builds": [
    {
      "src": "pom.xml", 
      "use": "@vercel/static-build",
      "config": { "distDir": "target/static" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
